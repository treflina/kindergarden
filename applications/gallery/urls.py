from django.urls import include, path
from . import views

app_name = "gallery_app"

urlpatterns = [
    #     path('galeria/test/<group>/', views.gallery_listing_view, name="gallery_listing_view"),
    #     path('galeria/<gallery_id>/', views.gallery_detail_view, name="gallery_detail_view"),
]
